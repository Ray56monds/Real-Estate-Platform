To generate a basic TypeScript entity for a property, you can use TypeORM, which is a popular ORM for TypeScript and JavaScript. Below is an example of how you might define a `Property` entity in a file named `property.entity.ts`.

```typescript
import { Entity, Column, PrimaryGeneratedColumn } from 'typeorm';
